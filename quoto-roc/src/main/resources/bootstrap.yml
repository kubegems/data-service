server:
  port: 8070
# 默认的profile为dev，其他环境通过指定启动参数使用不同的profile，比如：
#   测试环境：java -jar my-spring-boot.jar --spring.profiles.active=test
#   生产环境：java -jar my-spring-boot.jar --spring.profiles.active=prod
spring:
  application:
    name: dataServer-quotoRoc
  cloud:
    nacos:
      discovery:
        server-addr: 172.16.31.162:8848
        group: bigdata
  redis:
    host: 172.16.24.231
    port: 6379
configDbUrl: jdbc:mysql://XXXXX:3306
configDbAccount: root
configDbPassword: 123456
dbUrl: jdbc:clickhouse://clickhouse.cloudminds.com:9090
dbAccount: distributed-read
dbPassword: cloudminds123