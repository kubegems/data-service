server:
  port: 8076
# 默认的profile为dev，其他环境通过指定启动参数使用不同的profile，比如：
#   测试环境：java -jar my-spring-boot.jar --spring.profiles.active=test
#   生产环境：java -jar my-spring-boot.jar --spring.profiles.active=prod
spring:
  cloud:
    nacos:
      config:
        group: bigdata-quoto-search.bj-fit-86
        prefix: search
        file-extension: yaml
        namespace: dataservice
      discovery:
        group: bigdata
  profiles:
    active: test
  application:
    name: dataServer-quotoSearch


---
#测试环境配置
spring:    
  profiles: test
  cloud:
    nacos:
      config:
        server-addr: 172.16.23.85:31215
      discovery:
        server-addr: 172.16.23.85:31215
        
---
#线上环境配置
spring:    
  profiles: online
  cloud:
    nacos:
      config:
        server-addr: nacos.cloudminds.com
      discovery:
        server-addr: nacos.cloudminds.com