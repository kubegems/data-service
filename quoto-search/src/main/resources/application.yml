server:
  port: 8076
spring:
  profiles:
    active: test
  application:
    name: dataServer-quotoSearch
  cloud:
    nacos:
      discovery:
        group: bigdata
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8