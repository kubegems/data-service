server:
  port: 8074
spring:
  application:
    name: dataServer-quotoManage
  profiles:
    active: dev
  cloud:
    nacos:
      discovery:
        group: bigdata
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8